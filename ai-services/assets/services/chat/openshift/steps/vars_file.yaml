containers:
  - name: "ui"
    format: ".Status"
    alias: UI_STATUS

  - name: "backend-server"
    format: ".Status"
    alias: API_STATUS

hosts:
  - fetch: UI_URL
    type: ui
  - fetch: API_URL
    type: api
