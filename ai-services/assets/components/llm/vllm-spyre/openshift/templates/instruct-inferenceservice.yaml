apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: instruct
  annotations:
    security.opendatahub.io/enable-auth: "false"
    serving.kserve.io/deploymentMode: RawDeployment
  labels:
    networking.kserve.io/visibility: cluster-local
    ai-services.io/component: {{ .Release.Name }}
    ai-services.io/template: {{ .Chart.Name }}
    ai-services.io/version: {{ default .Chart.AppVersion | quote }}
spec:
  predictor:
    automountServiceAccountToken: false
    deploymentStrategy:
      type: Recreate
    maxReplicas: 1
    minReplicas: 1
    model:
      modelFormat:
        name: vLLM
      args:
        - '--tensor-parallel-size=4 '
        - '--max-model-len={{ .Values.llm.model.maxModelLen }} '
        - '--max-num-seqs={{ .Values.llm.model.maxBatchSize }}'
        - --served-model-name={{ .Values.llm.model.name }}
      env:
        - name: VLLM_SPYRE_USE_CB
          value: "1"
      {{- if .Values.llm.apiKey }}
        - name: VLLM_API_KEY
          valueFrom:
            secretKeyRef:
              name: vllm-instruct-api-key
              key: apiKey
      {{- end }}
      resources:
        requests:
          cpu: {{ .Values.llm.resources.requests.cpu | quote }}
          memory: {{ .Values.llm.resources.requests.memory | quote }}
          ibm.com/spyre_pf: {{ index .Values.llm.resources.requests "ibm.com/spyre_pf" | quote }}
        limits:
          cpu: {{ .Values.llm.resources.limits.cpu | quote }}
          memory: {{ .Values.llm.resources.limits.memory | quote }}
          ibm.com/spyre_pf: {{ index .Values.llm.resources.limits "ibm.com/spyre_pf" | quote }}
      runtime: spyre-temp
      storageUri: {{ .Values.llm.storageUri }}
